# Copy to .env (gitignored) and set a strong password before `docker compose up`.
# The .NET connection string lives OUTSIDE the repo in user-secrets (see quickstart.md),
# and must use the same values you set here.
POSTGRES_USER=jobs
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=jobs
POSTGRES_PORT=5432
